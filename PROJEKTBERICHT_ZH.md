# 项目报告：路口多传感器激光雷达目标检测

**硕士论文 — 截至 2026 年 7 月 3 日。**

本报告从头到尾讲述了这个项目中所做的一切：数据是如何产生的，过程中发现并修复了
哪些错误，最终得到了什么结果，以及从中可以得出哪些结论。所有专业术语在第一次
出现时都会直接解释，因此即使没有任何背景知识也能读懂本报告。想深入了解细节的
读者可以查阅另外三份文档：`results/DATA_AUDIT.md` 包含对数据的技术审查和所有详细分析，
`results/ABLATION_BICYCLE.md` 描述了对比实验，`results/RESULTS.md` 是包含所有表格的简明结果
报告。（这三份文档为德文。）

---

## 1. 这个项目是关于什么的？

在一个路口固定安装了两台激光雷达（LiDAR）传感器。激光雷达是一种激光扫描仪：
它发射光脉冲，测量光到达障碍物再返回所需的时间，并据此为每个脉冲计算出空间中
的一个测量点。每秒数百万个这样的测量点汇聚成所谓的点云——可以把它想象成一张
只由一个个点组成的三维照片。

一个神经网络需要在这些点云中自动识别行人、自行车和汽车。对每个被找到的物体，
识别结果是一个所谓的边界框（Bounding Box）：一个假想的长方体，描述物体在哪里、
有多大、朝向哪个方向。

本论文的核心问题是：**把两台传感器的数据结合起来是否值得，还是单台传感器就够
了？** 为了回答这个问题，我们比较了三种方案。方案 "os0" 只使用第一台传感器，
方案 "os1" 只使用第二台传感器，方案 "merged"（融合）则使用两台传感器合并后的
点云。

## 2. 数据是如何产生的？

2025 年 10 月 9 日录制了九次实验。每次实验中恰好有一个已知的目标物体穿过路口：
实验 1 至 3 中是一辆汽车驶过场景，实验 4 至 6 中是一辆自行车，实验 7 至 9 中是
一个行人。此外，在整个录制过程中，场景里还停着一些汽车和其他静止物体。

传感器每秒采集十次完整的点云。这样每种传感器方案总共产生了 3327 个采样（下文
称为"帧"）。

## 3. 两台传感器的数据是如何合并成一幅图像的？

每台传感器都从自己的视角、在自己的坐标系中测量世界——也就是说，每台传感器都
把自己当作原点。因此在合并两片点云之前，必须先把它们精确地对齐叠放。这个过程
称为 Merge（英文"合并"之意）。对每一帧，它分四步进行：

1. 加载两片点云，去除明显的测量噪声。这一步由统计离群点滤波器完成：如果某个点
   离它所有的邻近点都异常远，那它很可能是测量误差，会被剔除。
2. 将其中一片点云粗略地旋转和平移到另一片的坐标系中。所需的旋转和平移描述了两
   台传感器彼此的相对位置，称为外参（Extrinsik），它事先由人工一次性标定。
3. 接着由一个名为 ICP（Iterative Closest Point，迭代最近点）的算法完成精细工作：
   它一小步一小步地相互移动两片点云，直到它们尽可能好地重合。
4. 最后把两片点云的点简单地拼接成一片共同的点云。

在我们的处理流程中，这个过程平均每帧耗时约 2.3 秒。听起来很慢，但这并不是该方
法的根本问题：几乎所有时间都花在噪声滤波和 ICP 精对齐上。由于传感器是固定安装、
不会移动的，精对齐其实只需要做一次。在实时系统中，每帧只剩下旋转和拼接，只需
几毫秒。详见第 11 节。

## 4. 怎么知道什么是"正确答案"？——标注

要让神经网络学习，并且要评价它的表现，就需要可供对照的"正确答案"。这个正确
答案称为 Ground Truth（真值）：由人查看每一帧，亲手在每个真实物体周围画出一个
边界框。这个过程称为标注（Labeling），画出的标记称为标签（Labels）。

本项目为此专门开发了一个标注程序（`experiment/manual_bbox_editor.py`）。标注
时遵守了几条重要约定：

- 共有三个物体类别：行人、自行车、汽车。
- 每个框额外带有一个名为 "static" 的属性，表明该物体是静止的（例如停放的汽车）
  还是在运动。这个区分在第 9 节会变得非常重要。
- 由于各种原因不可用的帧被标记为无效，共涉及 2521 帧。我们专门验证了这些帧既没
  有进入训练，也没有进入评估。

## 5. 训练前的准备：数据格式与数据划分

训练使用了免费开源的软件框架 MMDetection3D，这是一个 3D 目标检测的工具箱。该
框架要求数据采用一种特定格式，即 KITTI 格式。KITTI 是道路交通目标检测领域最著
名的公开数据集，它的数据格式已成为事实标准。我们专门编写的转换程序把点云旋转、
平移到一个类似 KITTI 的坐标系中，并写成框架所要求的文件格式。

随后对数据进行了划分。神经网络绝不能用它训练时见过的数据来评价——否则只是在
检验它有没有背下来。惯例是把数据分成三部分，称为 Splits：

- **训练集（80%）：** 网络用这些数据学习。
- **验证集（10%）：** 训练过程中用这些数据阶段性地检查哪个训练状态最好。
- **测试集（10%）：** 这些数据直到最后都不被触碰，用于给出最终的、未被粉饰的
  评价。

划分是按时间进行的：每个实验的前 80% 时间段作为训练集，最后 10% 作为测试集。
这样训练就永远无法"偷看"测试的未来。我们还检查了没有任何一帧同时出现在两个
集合中。测试数据渗入训练的现象称为 Leakage（泄漏），它会人为美化结果，在这里
被排除了。

## 6. 数据审查：找到并修复了三个隐藏的错误

在信任结果之前，我们对整条数据处理链进行了系统检查。这次审查（"Audit"）发现
了三个真实存在的错误，全部得到了修复。

**错误 1：地面被切掉了。** 网络只处理一个固定空间范围内的点，其中一个条件是：
点必须高于坐标原点以下 3 米。但由于转换中的一个错误平移值，我们场景的地面落在
了原点以下 3.44 米处——也就是在这个范围之外。于是一个过滤器把整个地面、连同
每个物体底部约 40 厘米一起删掉了。网络在训练中看到的是没有脚的人和没有轮子的
汽车。修正之后，地面回到了网络在预训练中习惯的高度。

**错误 2：所有的框都悬在空中。** 标注程序把框的高度位置存成框的中心点，而训练
框架把同一个数值理解为框的底边。这导致所有训练框都比实际位置高出半个框的高度
——对汽车来说约 75 厘米。这个错误在转换程序中得到了修正，并经过统计验证。

**错误 3：反射强度值不可比。** 每个测量点除了位置外还带有一个强度值——表示表
面反射激光的强弱。这些值原本在每一帧中都按当帧最亮的点来缩放。后果是：同一个
表面在每一帧中得到的数值都不一样。现在改为除以一个固定的数，使数值在所有帧中
含义一致。

在这些修正之前产生的所有结果都被宣布作废。修正后的数据状态称为 "v2"，本报告中
的所有数字都基于它。

## 7. 神经网络的训练

检测模型采用了 PointPillars，这是一个成熟且特别快速的 3D 目标检测神经网络。其
基本思想是：网络从上方俯视场景，把场景划分成一个由竖直柱子组成的网格（英文
"pillars"，柱子，因此得名）。每根柱子内的所有点被汇总成一段紧凑的描述。然后一
个图像识别网络在这个网格上工作，预测出边界框。框的起点是所谓的 Anchors（锚框）：
按照汽车、行人或自行车的典型尺寸预先定义的参考框，网络只需再对它们做平移和尺
寸调整。

网络不是从零开始训练的。我们使用了一个 Checkpoint（检查点）作为起点——检查点
指一个已训练网络的保存状态。我们的起点是一个在 KITTI 数据集上预训练好的
PointPillars，它已经大体知道汽车、行人和骑车人在激光雷达数据中长什么样。然后
把这些先验知识调整适配到我们的场景上。这种做法称为 Finetuning（微调）。训练共
进行了 50 个 Epoch；一个 Epoch 表示网络把所有训练数据完整看过一遍。

每个 Epoch 结束后，都在验证集上测量网络当前的识别水平，并自动保存表现最好的
中间状态。直到最后，才把这个最佳状态在测试集上应用一次——这唯一一次测量的结
果就是报告中的测试成绩。这种严格流程确保测试数字没有被反复尝试所美化。

## 8. 识别能力如何度量？以及度量方法中的错误

要判断一个预测框是否命中，需要用所谓的 IoU（Intersection over Union，交并比）
把它与对应的真值框比较。IoU 是两个框重叠部分的体积与两个框合起来所占体积之比。
IoU 为 1.0 表示完全重合，为 0 表示毫无接触。

识别能力用 Average Precision（AP，平均精度）表示。AP 后面的数字表示预测被算作
命中所需的 IoU 门槛：AP30 要求至少 30% 的重叠，主要衡量物体有没有被找到；AP60
要求 60% 的重叠，衡量框是否摆放精确。总指标 mAP（mean Average Precision）是四
个门槛（0.3 / 0.4 / 0.5 / 0.6）和三个类别的平均值。

网络的每个预测还带有一个 Score（置信度分数）：一个 0 到 1 之间的数，表示网络对
这个框有多确信。AP 的计算不使用固定的分数门槛——而是把所有预测按分数排序，对
整个排序进行评价。这恰好惩罚了该罚的情况：如果一个网络的错误都是低分、命中都
是高分，它几乎不丢分。

**原始的度量实现中藏着一个严重错误。** AP 被逐帧单独计算，然后对所有帧取平均
——包括那些根本不含有目标类别的帧，这些帧按"0 分"计入平均。由于 266 个测试
帧中只有 70 帧含有自行车，自行车的数值在数学上永远不可能超过 0.24——无论网络
有多好。而实际上网络找到了全部 70 辆自行车。这个错误之所以被发现，是因为两个
训练方式不同的模型得到了完全相同的自行车分数——恰好正是这个错误算法的理论上
限值。此后度量方法被改成了通行的标准做法：把整个测试集的所有预测放在一起统一
评价。

这个错误从未让网络变差——但它长期掩盖了网络真实的水平。

## 9. 结果

### 9.1 测试集上的总体结果

下表展示了所有训练变体在测试集上的表现。列中的缩写代表三个类别：行人（p）、
自行车（b，bicycle）、汽车（c，car）。"GT-Sampling" 和 "Oversampling" 是两种
训练变体，将在第 10 节解释。

| 模型 | mAP | AP30 p/b/c | AP60 p/b/c |
|---|---|---|---|
| **merged + GT-Sampling** | **0.8916** | 0.903 / 0.972 / 0.909 | 0.758 / 0.883 / 0.814 |
| merged 基线 | 0.8880 | 0.900 / 0.935 / 0.909 | 0.772 / 0.856 / 0.816 |
| merged + Oversampling | 0.8788 | 0.901 / 0.890 / 0.909 | 0.856 / 0.881 / 0.812 |
| os1 + GT-Sampling | 0.8570 | 0.902 / 0.863 / 0.909 | 0.623 / 0.791 / 0.909 |
| os0 + GT-Sampling | 0.8412 | 0.863 / 0.982 / 0.997 | 0.654 / 0.496 / 0.908 |

在统一训练配置下，排名为 merged 高于 os1 高于 os0。这符合"每个物体上的测量点
越多，识别越容易"的预期。

### 9.2 为什么这些数字有水分——以及更诚实的评价方式

观察场景时出现了一个重要疑问：测试区域停满了汽车。测试集中 1583 个汽车框里有
1548 个属于停放的汽车——而正是这些汽车，早已以同样的位置、同样的姿态出现在训
练数据里。网络其实不需要真正"识别"它们，只需要"认出"它们。这类轻而易举的
命中把总分抬高了。

因此我们额外做了分开的评价：一次只针对运动物体，一次只针对静止物体。运动物体
才是这个系统的真正任务。运动物体的结果（分别为 AP30 / AP60）：

| 模型 | 行人 | 自行车 | 汽车 |
|---|---|---|---|
| merged + GT-Sampling | 0.90 / 0.71 | **0.97 / 0.88** | **0.90** / 0.19 |
| merged 基线 | 0.89 / 0.71 | 0.94 / 0.86 | 0.70 / 0.13 |
| os0 + GT-Sampling | 0.73 / 0.35 | 0.98 / 0.50 | 0.74 / 0.68 |
| os1 + GT-Sampling | 0.90 / 0.46 | 0.86 / 0.79 | **0.09 / 0.09** |

### 9.3 本论文的主要结果

这张表里藏着最重要的发现：**传感器 os1 几乎完全找不到行驶中的汽车。** 它 0.09
的数值意味着它的预测几乎从不算作命中——预测框平均偏离真实汽车一米半，尽管汽
车在它的数据中清晰可见（平均 420 个测量点）。特别有说服力的是：即使采用找到的
最佳训练配置，os1 也几乎没有改善（从 0.02 到 0.09）。所以这个问题无法通过更好
的训练解决——它源于几何：os1 只能从一侧看到汽车，因此系统性地无法正确确定它
的位置。

**两台传感器的融合恰好解决了这个问题：数值跃升到 0.90。** 这就是本论文的核心、
经过实验验证的论断——对运动物体而言，融合两个视角不是锦上添花，而是必需。

### 9.4 定位：与前一篇工作的对比

针对完全相同的实验装置，存在一篇前置研究：Thea Pagel 的硕士论文（2025 年，
PDF 存放在项目文件夹中）。她使用了同样的地下车库录制数据——同样的两台传感
器、同样的场景、同样的点云合并软件。关键区别在于：她**没有在场景数据上对神经
网络进行再训练（微调）**，而是直接把在其他数据集上预训练好的现成网络应用到这
些录制数据上。

这种做法的结果并不理想。她表现最好的网络（在道路交通数据集 KITTI 上预训练）
在融合数据上的总分只有 0.058——而满分是 1.0。我们对同一个网络在同样的数据上
做微调之后，达到了 0.8916，相差大约 15 倍。公平起见需要说明：这两个数字不能
精确到小数点后逐位比较，因为前一篇工作的参考框是自动生成的（没有像我们那样
做人工修正），并且使用了她自己实现的一套度量方法。但这不影响差距的数量级。

由此得出整个项目链条上或许最重要的实践经验：**把一个预训练网络直接套用到新
场景上是行不通的——在目标场景的数据上做微调才是决定性的一步。** 另外一个有
趣的事实是，两篇工作各自独立地得出了同样的融合结论：在前一篇工作中，即使不
做微调，融合两台传感器也比任何单台传感器好 1.4 到 3.4 倍；在微调之后，这个优
势依然存在，并且恰恰在最重要的地方表现得最明显：行驶中的汽车（单传感器 os1：
0.09——融合：0.90）。

## 10. 对比实验（消融实验）

消融实验（Ablation）指的是一种受控实验：只改变一个变量，其余全部保持不变，从
而干净地测量这一个改动的效果。共进行了四组这样的实验。

**实验 A：GT-Sampling——胜出者。** 训练数据中每一辆自行车对应约 22 辆汽车。
类别如此失衡时，网络对稀有类别学得较差。作为对策，训练时把其他训练帧里的自行
车和行人实例复制粘贴到当前场景中——这是一种标准技术，称为 GT-Sampling 或复制
粘贴增强。对严谨性至关重要的是：被复制的实例全部来自训练集，绝不来自验证或测
试数据。结果：自行车识别从 0.935 提高到 0.972，行驶汽车的识别从 0.70 提高到
0.90——而其他任何指标都没有变差。这个变体被采纳为标准。

**实验 B：帧过采样（Oversampling）——被否决。** 更直观的替代方案：训练时把含
有自行车的帧简单地多展示一倍。结果毫无收益——总成绩甚至低于基线。解释是：单
纯重复同样的帧并不会产生网络可以学习的新情形。（出于透明性说明：这次训练在第
37 个 Epoch 时崩溃了，原因是在训练运行期间更换了服务器上的度量代码——一个本
可避免的操作失误。训练从最后一个中间状态恢复完成；这一局限记录在
`results/ABLATION_BICYCLE.md` 中，不影响否定性结论。）

**实验 C：跨类别去重——无效果，但无害。** 网络有时会对同一堆点同时报告多种解
读，例如"汽车"里面又叠着一个"自行车"。常规的清理机制（Non-Maximum
Suppression，NMS，非极大值抑制）只在同一类别内部去除重复。我们测试了一条附加
规则：如果一个框几乎完全位于另一个类别的、更高置信度的框之内，就将其丢弃。这
条规则在 2353 个预测中删掉了 11 个，且没有改变任何一个指标。它无害且让输出看
起来更干净，但对指标没有可测的贡献。

**实验 D：地下点过滤——假说被推翻。** 数据中偶尔出现地面以下的点团，推测是潮
湿路面上的镜面反射。假说是：这些"镜像点"会诱发误检。为验证，我们删除了所有
低于原点 2 米的点（只占全部点的 0.09%），并重新训练了网络。结果推翻了假说：误
检没有变少（58 个对 42 个），而行驶汽车的识别反而变差了（0.63 对 0.90）。因此
这个过滤器没有被采纳。这样的否定性结果同样有价值：一个貌似合理的解释被提出、
被有针对性地检验、被干净地排除。

## 11. 速度：系统能实时运行吗？

推理（Inferenz/Inference）指把训练好的网络应用到新数据上：点云进去，边界框出
来。推理时间就是这样一次运算的耗时。它在计算服务器的显卡（一块 Tesla V100）上
测得，包含数据加载和预处理。

实时性的计算是这样的：传感器每秒提供十帧，也就是每 100 毫秒到来一片新点云。如
果系统在下一帧到来之前就处理完当前帧——即耗时低于 100 毫秒——它就是实时的。

| 方案 | 每帧耗时 | 预算 | 结论 |
|---|---|---|---|
| merged（融合） | 85 毫秒 | 100 毫秒 | 实时，余量 15 毫秒 |
| os0 单传感器 | 45 毫秒 | 100 毫秒 | 实时 |
| os1 单传感器 | 43 毫秒 | 100 毫秒 | 实时 |

融合的计算量约为单传感器的两倍——很合理，因为要处理的点数翻倍。关键在于：
**即使是融合也保持在 100 毫秒预算之内，因此是实时可行的。**

完整的图景还包括合并步骤（第 3 节）。在我们的离线处理中它每帧耗时 2.3 秒——
远超预算。但这个数字并不代表实时运行的情况：它包含了 ICP 精对齐，而对固定安装
的传感器来说，精对齐只需做一次。实时运行时每帧只剩旋转和拼接，只需几毫秒。整
个系统因此仍在时间预算之内。

## 12. 三个尚存的弱点——以及它们的成因

没有完美的结果。系统的三个弱点都被追查到了根源。

**弱点 1：行驶汽车的框偏小。** 行驶中的汽车几乎总能被找到（0.90 的 AP30 证明了
这一点），但更严格的 AP60 只有 0.19——框摆放得不够精确。误差分解显示：位置几
乎完美（横向只偏 9 厘米），但预测框平均短了 95 厘米、窄了 41 厘米。原因是：传
感器对行驶中的汽车永远只能看到一部分（测量点只覆盖车长的约 60%），在这种不确
定中，网络退回到它在 KITTI 预训练中学到的典型汽车尺寸——而 KITTI 的汽车比我们
标注的目标车（4.95 米 × 1.95 米）小。最初怀疑的另一种解释——汽车的运动导致
融合点云"拖影"，因为两台传感器的采集时刻略有错位——经检验被排除：错位平均
只有 9 厘米。还有一个现实核对待完成：如果目标车的真实尺寸比标注框小，那么这个
"弱点"有一部分其实是标注时的测量误差。

**弱点 2：网络对运动物体不够自信。** 对行驶中的汽车，置信度分数平均为 0.74，
而对停放的汽车为 0.98。在 35 个案例中有 10 个低于 0.6。实际后果：不存在一个单
一的分数门槛，既能压制所有误检、又能在每一帧都保住目标车。现实系统因此需要按
类别设置不同的门槛，或者跨帧进行时间上的目标跟踪（Tracking），用它来跨越个别
不自信的帧。

**弱点 3："幽灵自行车"。** 网络偶尔在没有自行车的地方报告自行车（测试中 42
例，全部是 0.3 到 0.6 之间的低分）。对这些案例逐帧追查发现了三种成因。第一：
其中一些"错误"落在真实存在、带有数百个测量点、但从未被标注过的静止物体上
——那里到底立着什么（可能真的是停放的自行车！），只有去现场才能弄清。第二：
猜测中的镜面反射伪影——这个解释已被实验 D 推翻。第三，也最有意思：有些框立在
完全空旷的地面上，却精确到厘米地落在训练期间自行车经过的路线上。也就是说，网
络不仅记住了自行车长什么样，还记住了它们**通常出现在哪里**——如今便偶尔在这
些位置无端报告自行车。这是所有数据来自同一个场景的直接后果，也是这套数据基础
最重要的局限。

## 13. 产出的工具

除了训练结果之外，还产生了若干可复用的工具：

- 一个**交互式 3D 查看器**（`experiment/prediction_viewer.py`），可以逐帧浏览
  采样，同时看到点云、人工标注的标签和网络的预测。显示门槛可以按物体类别分别
  调节；使用 `--full` 选项可以浏览完整实验，而不仅是测试片段。
- 论文用**插图**（位于 `results/figures/` 目录）：鸟瞰视角的示例场景（其中包
  括同一时刻"os1 错过汽车 / merged 命中汽车"的直接对比）、精确率-召回率曲线
  （Precision-Recall）以及置信度分布图。
- **分析脚本**（位于 `mmdetection3d/tools/analysis_tools/`），用于运动/静止分
  离评价、误差分解、跨类别去重以及生成全部插图。
- 全部工作用 Git 做了版本管理并备份到 GitHub（仓库
  `DieserLaurenz/lidar-ojbect-detection-parking-lot`）。

## 14. 结论

从这个项目中可以证明五条论断：

1. **对运动物体而言，传感器融合是必需的，而不只是有帮助。** 视角不利的单台传
   感器会系统性地错误确定行驶汽车的位置，再好的训练也无法改变这一点。融合两台
   传感器解决了这个问题——同时仍然快到足以实时运行。
2. **GT-Sampling 是对抗类别失衡最有效的训练手段。** 两种替代方案（重复帧、过
   滤地下点）都经过了测试并被有理有据地否决。
3. **只有把运动物体和静止物体分开评价才有说服力。** 总体数值被背下来的停放汽
   车撑高，夸大了真实水平。
4. **数据质量和度量质量决定一切。** 如果没有修复那四个错误（地面、框高度、强
   度、度量方法），所有数字都会是错的——自行车识别看起来一塌糊涂（0.24），实
   际上非常出色（0.94）。
5. **尚存的弱点已被理解并可以解释**——部分可见物体上的框偏小、运动物体上的置
   信度偏低、以及源于单一场景数据的位置性误检。

## 15. 工作的局限与展望

诚实的评价离不开局限性：所有数据来自同一个场景。物理上只有三辆不同的自行车、
一辆目标车和少数几个人。测试中只包含 35 次对行驶汽车的观测，而且汽车在测试段
移动缓慢（约每秒 2 米）。停放的汽车在训练和测试中完全相同。这些因素限制了数字
的可推广程度，应当透明地写进论文。

面向未来的方向有：为固定场景建立背景地图，以抑制位置性误检；跨帧的目标跟踪
（Tracking）；把合并步骤改造成一次性标定、供实时运行使用；以及最重要的——采
集更多场景、更多物体多样性的数据。

有两个小问题只能在现场澄清：目标车的真实尺寸是多少（与 4.95 × 1.95 米的标注框
核对）？在传感器附近、网络固执地报告自行车的那两个位置上，立着的到底是什么
——会不会真的是从未被标注过的停放自行车？
